# خطة التطوير

## المرحلة 1 — تشغيل حقيقي (أسبوعان)
- [ ] تشغيل Anthropic في الإنتاج مع streaming للردود (SSE في `/agent/chat`).
- [ ] Embeddings حقيقية (Voyage multilingual) + إعادة فهرسة تلقائية عند تغيير النموذج.
- [ ] استبدال mock tools بتكاملات محسومة كلاود (projects, billing) وDCC (KPIs) خلف نفس المخططات.
- [ ] تذاكر حقيقية (Zendesk/Freshdesk) + إشعار الفريق عند `handoffRequired`.
- [ ] Redis rate limiting + أخذ `tenantId` من JWT موقّع بدل الثقة بالحقل عند استخدام الويدجت مباشرة.

## المرحلة 2 — جودة الإجابات
- [ ] Re-ranker (cross-encoder) فوق البحث الهجين.
- [ ] تقييم آلي (eval set لكل منتج) يقيس الدقة والاقتباس والامتناع، يعمل في CI بالمزوّد الحقيقي.
- [ ] تعليقات المستخدم (👍/👎) على كل رد وربطها بالتحليلات.
- [ ] كشف تكرار الأسئلة منخفضة الثقة واقتراح مقالات معرفة تلقائياً.
- [ ] ذاكرة مستخدم قصيرة (تفضيلات، مشروع حالي) مع سياسات احتفاظ.

## المرحلة 3 — منصة للبيع
- [ ] مستخدمون متعددون في لوحة التحكم + SSO + سجل تدقيق كامل.
- [ ] Workspaces (مالك المنصة ← عملاء ← منتجاتهم) وفوترة بالاستخدام.
- [ ] PostgreSQL + pgvector، وملفات في S3 متوافق (Object storage داخل المملكة).
- [ ] Webhooks للأحداث (محادثة جديدة، تحويل، تذكرة) + واجهة MCP لعرض الأدوات لعملاء خارجيين.
- [ ] Versioning لملفات المنتج والبرمبت مع rollback.
- [ ] قنوات إضافية: WhatsApp Business، Slack، البريد.

## المرحلة 4 — الاستقلالية
- [ ] Agents متعددة الخطوات مع تأكيد المستخدم للإجراءات ذات الأثر (approval flow في الويدجت).
- [ ] تحليلات نية العميل (مبيعات/دعم/onboarding) وتوجيه للفريق المناسب.
- [ ] Fine-tuned/ prompt-tuned شخصية لكل منتج بناءً على المحادثات المعتمدة.
